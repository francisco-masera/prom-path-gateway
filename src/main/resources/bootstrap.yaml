spring:
  application.name: gateway
  profiles.active: dev
  cloud:
    #    vault:
    #      token: ${VAULT_TOKEN}
    #      authentication: token
    #      uri: http://vault:8200
    #      kv:
    #        default-context: gateway
    #        backend: secret
    config:
      enabled: true
      fail-fast: true
      uri: http://config-server:8888
      retry:
        max-attempts: 5
        max-interval: 1500
server.port: 8080
feign:
  auth-svc:
    url: "auth"
    port: "8080"
    host: "http://auth-service"
    sign-up-url: "sign-up"
    login-url: "login"
#azure:
#  keyvault:
#    enabled: true
#    uri: https://first-ms-vault.vault.azure.net/
#    client-key: YyJ8Q~MnRrp32V6yu5hHJHj4XltV~~sWOFSFtaAf
#    client-id: e7d16864-1f98-4e5b-a99a-e523598b3ebe
#    tenant-id: d514b9c0-d478-40e3-b096-69e5874722c0